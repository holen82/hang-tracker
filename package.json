{
  "name": "hang-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:all": "vitest run && playwright test"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "@playwright/test": "^1.44.0",
    "serve": "^14.0.0"
  }
}
